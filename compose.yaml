services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kookia
      POSTGRES_USER: kookia
      POSTGRES_PASSWORD: kookia_dev
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - kookia-postgres:/var/lib/postgresql/data

volumes:
  kookia-postgres:
